import 'driver-dash/ext/zf-accordion';

export function initialize(/* application */) {
  // application.inject('route', 'foo', 'service:foo');
}

export default {
  name: 'zf-accordion',
  initialize
};
